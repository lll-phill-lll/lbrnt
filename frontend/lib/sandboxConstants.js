/** Одна фиксированная «комната» для dev-песочницы (без лобби). */
export const SANDBOX_ROOM_ID = 'sandbox';
/** Токен в rooms/sandbox.json — нужен для save-scenario (запись в tests/scenarios). */
export const SANDBOX_CREATOR_TOKEN = 'dev-sandbox-token';
